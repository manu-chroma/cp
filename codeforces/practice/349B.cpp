#include <bits/stdc++.h>
using namespace std;

typedef long long int li;

li v[1000005] ;

int main(int argc, char const *argv[])
{
	ios_base::sync_with_stdio(false); 

	li n;
	cin >> n;
	int a[10];
	for (int i = 1; i <= 9; ++i)
	{
		cin >> a[i];
	}

	for (int i = 1; i < n; ++i)
	{
		v[i] = 0;
		for (int j = 0; j < i; ++j)
		{
			
		}
	}
	return 0;
}
