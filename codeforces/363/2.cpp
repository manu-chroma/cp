#include <bits/stdc++.h>
using namespace std;

typedef long long int li;

int main(int argc, char const *argv[])
{
	li a, b;
	cin >> a >> b;
	char m[1001][1001];
	for (int i = 1; i <= a; ++i)
	{
		for (int j = 1; j <= b; ++j)
		{
			cin >> a[i][j];
		}
	}

	bool row = 0, col = 0;
	for (int i = 0; i < a; ++i)
	{
		
	}

	return 0;
}