#include <bits/stdc++.h>
using namespace std;

typedef long long int li;

int main() {
	ios_base::sync_with_stdio(false);

	int num[6];
	for (int i = 0; i < 6; ++i)
	{
		cin >> num[6];
	}
	int participants;
	cin >> participants;

	for (int i = 0; i < participants; ++i)
	{
		string s;
		cin >> s;
		if(s == 1) {

		}

	}
	return 0;
}