#include <bits/stdc++.h>
using namespace std;

typedef long long int li; 

int main() {
	ios_base::sync_with_stdio(false);

	int petr = 1;
	int last_winner = 0;
	int n;
	cin >> n;
	while(n--) {
		int num, last = 2;
		cin >> num;

		if()

		// if(last == 1) {
		// 	petr = 1;

		// }
		if(num%2 == 0 && !petr)
			cout << 1 << "\n";
		else 
			cout << 2 << "\n";

		starter = 0;
		last = num;
	}


	return 0;
}