#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
	string a = "yolo";
	cout << "length of string is " << a.length() << endl;
	for (int i = 0; i < a.length(); ++i)
	{
		cout << a[i] ;
	}
	cout << endl << a << endl;
	return 0;
}
