#include<bits/stdc++.h>
using namespace std;

#define MOD 4294967296

int binary_search()
int main() {
    long long int n, a, b, s;
    vector<int> array;
    cin >> n >> s >> a >> b;
    for(int i = 0 ; i < n ; i++ ) {
        s = (a*)
        /// typecast for floor function
        si = floor()
        if(s %2 != 0) {
            /// add number to a set if already not there
        }

        else {
            /// remove number from a set if it's there
        }
    }

}
